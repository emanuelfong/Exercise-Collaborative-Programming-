
#You have a list of courses. You want to put your list of courses 
# in a list with the INST classes coming first.
lambda x: 0 if x.starts with ("INST") else ord(x[0])


#We want to order a list of names where the names that have the same 
# number of characters as "Emily" apperar first.
len(set(string) and set("Emily"))

#LIst comprenhension syntax
[expression for intervariable in iterable]

#Escape sequence for tab charater
\t 